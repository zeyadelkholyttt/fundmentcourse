#include "iostream"
using namespace std;
int main (int argc, char *argv[]) {
  int sum=0, num, count;
  while (true) {
    cout << "enter the num: ";
    cin >> num;
    if (num<0 || count ==3{
      break;
    }
    sum+= num;
    count++;
  }
  cout << "the final sum: " << sum << endl;
  cout << "entered nums: " << count;
  return 0;
}
